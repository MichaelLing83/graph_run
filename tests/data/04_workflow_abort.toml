# Task fails → default failure branch to abort; graph_run must exit nonzero.

[[nodes]]
id = "fail-to-abort"

[[edges]]
from = "start"
to = "fail-to-abort"

[[edges]]
from = "fail-to-abort"
to = "end"
